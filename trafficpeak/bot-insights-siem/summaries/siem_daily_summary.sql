SELECT
  toStartOfDay (timestamp) AS timestamp,
  method,
  status,
  country,
  IF(
    cityHash64 (clientIP, timestamp, requestId) % 100 < 10,
    clientIP,
    '~~~SAMPLED_OUT~~~'
  ) AS clientIP,
  IF(
    cityHash64 (host, timestamp, requestId) % 100 < 10,
    host,
    '~~~SAMPLED_OUT~~~'
  ) AS host,
  IF(
    cityHash64 (path, timestamp, requestId) % 100 < 10,
    path,
    '~~~SAMPLED_OUT~~~'
  ) AS path,
  ruleMessages,
  ruleTags,
  ruleActions,
  attackTypes,
  ruleTagsWAF,
  attack_waf,
  ruleTagsDOS,
  attack_dos,
  ruleTagsBot,
  attack_bot,
  rules,
  asn,
  CH_UA,
  has_CH_UA,
  UA,
  attack_reputation,
  ruleMessage,
  protocol,
  requestId,
  ruleSelectors,
  ruleData,
  count() AS request_count
FROM
  __PROJECT_NAME__.__TABLE_NAME__
GROUP BY
  timestamp,
  method,
  status,
  country,
  clientIP,
  host,
  path,
  ruleMessages,
  ruleTags,
  ruleActions,
  attackTypes,
  ruleTagsWAF,
  attack_waf,
  ruleTagsDOS,
  attack_dos,
  ruleTagsBot,
  attack_bot,
  rules,
  asn,
  CH_UA,
  has_CH_UA,
  UA,
  attack_reputation,
  ruleMessage,
  protocol,
  requestId,
  ruleSelectors,
  ruleData SETTINGS hdx_primary_key = 'timestamp'
